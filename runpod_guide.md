# 🚀 Runpod GPU 실행 및 과금 방지 가이드

Runpod은 사용한 시간만큼 비용이 차감되는 종량제 클라우드 GPU 서비스입니다. 본 세션에서는 실습을 위해 계정당 **$10의 크레딧**이 제공됩니다. 

실습이 끝난 후 인스턴스(Pod)를 제대로 종료하지 않으면 크레딧이 모두 소진되어 이후 과정에 차질이 생길 수 있으니, 아래 가이드를 반드시 숙지해 주시기 바랍니다.

---

## 💻 1. 추천 GPU 사양

대규모 언어 모델(LLM) 구동이나 API 연동 등 AI 서비스 개발 실습 시, VRAM이 넉넉하고 가성비가 뛰어난 아래의 GPU를 추천합니다.

* **RTX 3090 (24GB VRAM)**: 시간당 약 $0.20 ~ $0.40 내외. LLM 기초 실습이나 토큰화(Tokenization) 테스트, 딥러닝 모델 학습에 가장 적합합니다.
* **RTX A4000 (16GB VRAM)**: 모델 크기가 크지 않은 실습의 경우 가장 저렴하게(시간당 약 $0.20 미만) 사용할 수 있는 가성비 옵션입니다.
* *💡 꿀팁: Secure Cloud보다 **Community Cloud**를 선택하시면 훨씬 저렴한 단가로 이용할 수 있습니다.*

---

## ▶️ 2. Runpod 실행 가이드 (Deploy)

1. Runpod에 로그인한 후, 좌측 메뉴에서 **Pods**를 클릭합니다.
2. 우측 상단의 **+ Deploy** 버튼을 클릭합니다.
3. 상단 탭에서 **Community Cloud**를 선택합니다.
4. 목록에서 추천 GPU(예: `1 x RTX 3090`)를 찾아 **Deploy** 버튼을 누릅니다.
5. **Select Template**: 실습 환경에 맞는 템플릿을 선택합니다. (보통 `RunPod PyTorch` 또는 `Ubuntu` 기본 템플릿을 사용합니다.)
6. **Customize Deployment** (선택): 필요한 경우 컨테이너 디스크(Container Disk)와 볼륨 디스크(Volume Disk) 용량을 조절합니다.
7. **Continue**를 누르고 최종 예상 요금을 확인한 뒤 **Deploy**를 클릭하면 GPU 할당이 시작됩니다.
8. Pod 목록에서 상태가 `Running`으로 바뀌면 **Connect** 버튼을 눌러 Web Terminal이나 Jupyter Lab, SSH 등으로 접속하여 실습을 진행합니다.

---

## ⏸️ 3. 일시 정지 (Stop) - ⚠️ 스토리지 과금 주의!

당일 식사 시간이나 짧은 휴식 시간 동안 임시로 실행을 멈출 때 사용합니다.

1. **Pods** 메뉴에서 현재 실행 중인 Pod의 상세 카드를 엽니다.
2. **Stop(■)** 버튼을 클릭하여 실행을 멈춥니다.
3. **🚨 주의사항**: Stop 상태에서는 GPU 사용 요금은 청구되지 않지만, 할당해 둔 **스토리지(디스크) 유지 비용은 시간당 계속 차감**됩니다. 다음 날 이후에 실습을 이어가거나 며칠 동안 사용하지 않을 예정이라면 반드시 아래의 **완전 종료(Terminate)**를 진행해야 합니다.

---

## 🛑 4. 완전 종료 및 삭제 (Terminate) - ⭐️ 가장 중요!

그날의 실습이 완전히 끝났거나, 다음 세션 전까지 사용하지 않을 때 **반드시 진행**해야 불필요한 크레딧 소모를 막을 수 있습니다.

1. **💾 데이터 백업**: 실습 종료 전, 작성한 코드나 중요한 데이터는 반드시 **개인 PC로 다운로드**하거나 **GitHub에 Push**하여 백업합니다. (Terminate 시 복구 불가)
2. **Pods** 메뉴에서 해당 Pod 카드의 우측 하단에 있는 **휴지통 아이콘(Terminate)**을 클릭합니다.
3. 팝업 창에서 **Yes**를 클릭하여 완전히 삭제합니다.
4. 목록에서 해당 Pod가 완전히 사라졌는지 확인합니다. Pod가 목록에서 지워져야 스토리지 비용을 포함한 **모든 과금이 100% 중단**됩니다.

---

## 💡 요약: 크레딧 지키는 3계명
1. **잠깐 쉴 때 (1~2시간 이내)**: `Stop` (GPU 요금 중단, 스토리지 요금만 발생)
2. **실습 종료 후 (다음 날 이후 사용)**: 데이터 백업 후 무조건 `Terminate (휴지통)` (모든 요금 중단)
3. 좌측 하단의 **Billing** 메뉴에서 현재 남은 **크레딧($) 수시로 확인하기!**
